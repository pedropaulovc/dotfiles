{{ if ne .platform "windows" -}}
#!/bin/sh
# Fires whenever the canonical AGENTS.md changes:
# AGENTS.md sha256: {{ include "dot_agents/AGENTS.md" | sha256sum }}
cat <<'EOF'

  ⚠  AGENTS.md changed — its contents were copied to Claude Code + the cowork workspace.
     Claude.ai / Claude Desktop *chat* custom instructions are account-level and
     are NOT managed by chezmoi. Update them by hand to match:
     https://claude.ai/settings/profile  →  Personal preferences

EOF
{{ end -}}
