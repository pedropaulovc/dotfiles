# Repo-only files, never applied to $HOME.
README.md
LICENSE

# Platform-scoped: each OS ignores the other's files so `apply` is a noop.
{{ if eq .platform "windows" }}
# Linux/WSL-only targets — skip on Windows.
.bashrc
.profile
.inputrc
.tmux.conf
.config/nvtop
.config/ccstatusline
reminder-update-claude-ai.sh
# extensionless symlink isn't runnable from Windows PATH (needs PATHEXT); use the .cmd shim there.
.local/bin/claude-creds
.local/bin/ccs
{{ else }}
# Windows-only targets — skip on Linux/WSL.
OneDrive
.local/share/swsettings
open-swsettings.ps1
reminder-update-claude-ai.ps1
.local/bin/claude-creds.cmd
.local/bin/ccs.cmd
{{ end }}
